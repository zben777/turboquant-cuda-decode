# Third-Party Notices

## vLLM TurboQuant source snapshot

The files under `vllm/` were extracted from the TurboQuant-related portions
of the vLLM source tree. They retain their original SPDX headers and are
licensed under Apache License 2.0 by the contributors to the vLLM project.
See `vllm/LICENSE` and `vllm/README.md`.

The files under `reference/` are flat, byte-identical snapshots of selected
files under `vllm/`. They are retained separately because the standalone
benchmark harness loads them without requiring a complete vLLM checkout.

Upstream project: <https://github.com/vllm-project/vllm>

The local source tree used to prepare this repository did not include Git
metadata, so an exact upstream commit identifier is not available in this
snapshot.


